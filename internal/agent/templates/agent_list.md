List every sub-agent dispatched from this session via the agent, agentic_fetch, or Workflow tools, running or recently finished, with its session ID, dispatching tool, label, and state. Use this to see what's currently in flight -- e.g. after firing off several parallel agent() calls in one turn -- without waiting for them all to return. Pass a session ID to AgentProgress for a detailed look at one of them.
