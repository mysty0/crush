List every background task dispatched from this session -- sub-agents (agent, agentic_fetch), background workflows (Workflow), and scheduled tasks (ScheduleCron, ScheduleWakeup) -- running or recently finished, with its ID, dispatching tool, label, and state. Use this to see what's currently in flight, e.g. after firing off several parallel agent() calls in one turn, without waiting for them all to return. Pass any listed ID to AgentProgress for a detailed look at that task.
