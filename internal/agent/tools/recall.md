Search long-term memory for durable facts about this project or the user.

Relevant memories are injected into your context automatically, so you rarely need to call this. Use it for an explicit lookup when you want to check whether something was recorded before — a past decision, a command, a convention — and it is not already in front of you.

Returns the most relevant stored facts, or nothing if none match.
